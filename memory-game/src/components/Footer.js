const Footer = (props) => {
	// const [score, SetScore] = useState();
	// const [highScore, SetHighScore] = useState();

	const style = {
		height: '4em',
		width: '100%',
		backgroundColor: 'grey',
		display: 'flex',
		justifyContent: 'center',
		alignItems: 'center',
	};

	return <header style={style}>Footer</header>;
};

export default Footer;
